header.header
  a.header__logo.logo(href='/')
    img.logo__image(src='/img/icons/logo-image-only.svg' alt='логотип Тайпотеки')
    span.logo__name Типотека
  if (!admin && !user)
    nav.header__nav
      ul.header__nav-list
        li.header__list-item
          a.header__registration(href='/register') Регистрация
        li.header__list-item
          a.header__enter(href='/login') Вход с паролем
    a.header__search.button.button--search(href='#' aria-label='поиск по сайту')
  if (!admin && user)
    nav.header__nav
      ul.header__nav-list
        li.header__list-item
          p #{user.firstName} #{user.lastName}
        li.header__list-item
          a.header__exit(href='/logout') Выйти
    a.header__avatar.avatar
      img(src=`${user.avatar ? user.avatar : '/img/icons/smile.svg'}` alt='аватар пользователя')
    a.header__search.button.button--search(href='#' aria-label='поиск по сайту')
  if (admin)
    button.button.button--colored.header__button-new Новая запись
    div.header__dropdown
      a.button.button--burger.header__burger(href='#') Открыть меню
      ul.navigation.header__navigation
        li.navigation__item
          a(href='/') Пункт меню
        li.navigation__item
          a(href='/') Пункт меню
        li.navigation__item
          a(href='/') Пункт меню
        li.navigation__item
          a(href='/') Пункт меню
    a.header__search.button.button--search(href='#' aria-label='поиск по сайту')
